export enum ServiceEvents {
  SERVICE_WILDCARD = 'service.*',
  SERVICE_CREATED = 'service.created',
  SERVICE_UPDATED = 'service.updated',
  SERVICE_DELETED = 'service.deleted',
  SERVICE_OVERRIDE_WILDCARD = 'service.override.*',
  SERVICE_OVERRIDE_CREATED = 'service.override.created',
  SERVICE_OVERRIDE_UPDATED = 'service.override.updated',
  SERVICE_OVERRIDE_DELETED = 'service.override.eleted',
}
